(provide 'jd:asm-mode-keybindings.el)

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; `asm-mode-map' --- `asm-mode.el'
(defun jd:asm-mode-map ()
  (setf (cdr asm-mode-map) nil))
(add-hook 'asm-mode-hook 'jd:asm-mode-map)
